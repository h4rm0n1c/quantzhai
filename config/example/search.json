{
  "_comment": "QuantZhai search config template. Copy to config/user/search.json and fill in your SearXNG base URL. Never commit config/user/search.json.",
  "schema": "qz.search.v1",

  "searxng": {
    "_base_url_comment": "Use http://127.0.0.1:8890 for the Agent API (retrieval metadata); use http://127.0.0.1:8888 for raw SearXNG.",
    "base_url": "http://YOUR_SEARXNG_HOST:PORT",
    "timeout_s": 15,
    "enabled": true
  },

  "defaults": {
    "profile": "auto",
    "language": "en",
    "safe_search": 0
  },

  "_profiles_comment": "Override profile definitions here. Omit to inherit from config/default/search.json.",
  "profiles": {},

  "_routing_comment": "Override per-turn limits here. Omit individual fields to use defaults.",
  "routing": {
    "_budgets_comment": "Budget limits read by web_search at runtime. Omit to use built-in defaults.",
    "max_searches_per_turn": 4,
    "max_page_opens_per_turn": 3,
    "max_results": 8,
    "low_result_fallback_threshold": 2,
    "dedup_by_canonical_url": true
  },

  "_disabled_comment": "Add engine names to disable them for your instance.",
  "disabled_engines": [],

  "compatibility": {
    "_comment": "Set legacy_policy_path to use an existing search-policy.json for web_search_profiles routing.",
    "legacy_policy_path": ""
  }
}
