#!/usr/bin/env bash
set -euo pipefail

source "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/qz-codex-common"

qz_prepare_codex_home
qz_resolve_codex_profile "${1:-high}"

exec codex "${QZ_CODEX_EXTRA_ARGS[@]}" -m "$QZ_CODEX_MODEL"
